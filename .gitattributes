# 줄바꿈을 LF 로 고정한다.
#
# Windows 에서 작업하는데 CI 는 리눅스다. autocrlf 로 체크아웃마다 CRLF 가 되면
# 스크립트가 생성한 파일(LF)과 커밋된 파일(CRLF)이 내용은 같은데 diff 가 난다.
# CI 의 샘플 표 드리프트 가드가 그걸 실제 드리프트로 오탐한다.
* text=auto eol=lf

# 바이너리로 취급 (줄바꿈 변환 금지)
*.png binary
*.jpg binary
*.ico binary
*.pdf binary
*.woff binary
*.woff2 binary
